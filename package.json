{
  "name": "cukurpro",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "bun run --filter '@cukurpro/*' dev",
    "build": "bun run --filter '@cukurpro/*' build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "bun run --filter '@cukurpro/*' typecheck && bun run typecheck:e2e",
    "typecheck:e2e": "tsc --noEmit -p e2e/tsconfig.json",
    "test": "bun run --filter '@cukurpro/*' test",
    "test:e2e": "./scripts/test-e2e.sh",
    "deploy:check": "sh -n scripts/backup.sh scripts/restore.sh && bash -n scripts/test-e2e.sh scripts/deploy.sh && docker compose --env-file deploy/production.env.example -f compose.production.yaml config --quiet",
    "check:release": "bun run check && bun run test:e2e && bun run deploy:check",
    "check": "bun run format:check && bun run typecheck && bun run test && bun run build",
    "db:generate": "bun run --cwd server db:generate",
    "db:migrate": "bun run --cwd server db:migrate",
    "db:seed": "bun run --cwd server db:seed"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
